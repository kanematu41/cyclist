<% if current_user.following?(user) %>
<%= link_to "フォロー外す", unfollow_path(user.id), method: :delete, remote: true, class: "btn btn-primary col-xs-3" %>
<% else %>
<%= link_to "フォローする", follow_path(user.id), method: :post, remote: true, class: "btn btn-success col-xs-3" %>
<% end %>